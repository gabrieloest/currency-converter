spring:
  datasource:
    name: ''
    password: ''
    url: jdbc:h2:mem:zooplus;DB_CLOSE_DELAY=-1
    username: zooplus
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    open-in-view: false
    hibernate:
      ddl-auto: create
    show-sql: true
  thymeleaf:
    cache: false

server:
  port: 9090